# Single-node mode: coordinator also acts as worker
coordinator=true
node-scheduler.include-coordinator=true

http-server.http.port=8080

# Discovery service points to self in single-node setup
discovery.uri=http://localhost:8080

# Query memory limits — tune to your available container RAM
query.max-memory=4GB
query.max-memory-per-node=2GB
